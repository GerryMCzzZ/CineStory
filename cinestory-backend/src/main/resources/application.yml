server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: cinestory-backend

  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/cinestory?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # Flyway Configuration
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

  # Redis Configuration
  data:
    redis:
      host: 172.19.222.17
      port: 6379
      password: 12345r
      database: 0
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 1000ms
      timeout: 5000ms

  # File Upload Configuration
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB

  # Task Configuration
  task:
    execution:
      pool:
        core-size: 8
        max-size: 16
        queue-capacity: 100
      thread-name-prefix: cinestory-task-

# Video Generation Configuration
video:
  # 生成服务配置
  generation:
    # Provider configurations
    providers:
      runway:
        enabled: true
        api-key: ${RUNWAY_API_KEY:}
        base-url: ${RUNWAY_BASE_URL:https://api.dev.runwayml.com/v1}
        max-concurrent: 3
        priority: 1
        max-retries: 3
        timeout: 600000
        max-duration: 18
      pika:
        enabled: true
        api-key: ${PIKA_API_KEY:}
        base-url: ${PIKA_BASE_URL:https://api.pika.art/v1}
        max-concurrent: 5
        priority: 2
        max-retries: 3
        timeout: 300000
        max-duration: 4
      luma:
        enabled: true
        api-key: ${LUMA_API_KEY:}
        base-url: ${LUMA_BASE_URL:https://api.lumalabs.ai}
        max-concurrent: 5
        priority: 3
        max-retries: 3
        timeout: 300000
        max-duration: 5
    # Default settings
    default-provider: runway
    default-aspect-ratio: "16:9"
    default-duration: 5
    poll-interval: 5000

  # 视频输出配置
  output:
    path: ${VIDEO_OUTPUT_PATH:./storage/output}
    width: 1920
    height: 1080
    fps: 24
    bitrate: 2M

  # 视频标准化
  normalize:
    enabled: true

  # 转场效果
  transition:
    enabled: false
    duration: 0.5
    type: fade
    # 支持的转场类型: fade, wipe, slide, fadeblack, fadewhite,
    # dissolve, pixelize, reveal, curtain, circlecrop, rectcrop

  # 片头片尾
  intro:
    enabled: false
    path: ${VIDEO_INTRO_PATH:./storage/assets/intro.mp4}
  outro:
    enabled: false
    path: ${VIDEO_OUTRO_PATH:./storage/assets/outro.mp4}

  # 提示词配置
  prompt:
    max-length: 500

# LLM Configuration
llm:
  enabled: ${LLM_ENABLED:false}
  provider: openai
  api-key: ${LLM_API_KEY:}
  model: gpt-4
  base-url: ${LLM_BASE_URL:https://api.openai.com/v1}
  timeout: 120000
  max-retries: 3

# MinIO Configuration
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket-name: cinestory
  connect-timeout: 10000
  write-timeout: 60000
  read-timeout: 10000

# WebSocket Configuration
websocket:
  allowed-origins: "*"

# File Upload Configuration
file:
  upload:
    path: ${FILE_UPLOAD_PATH:./storage/uploads}
    max-size: ${FILE_MAX_SIZE:52428800}

# Logging Configuration
logging:
  level:
    root: INFO
    com.cinestory: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/cinestory.log
    max-size: 100MB
    max-history: 30

# Management Endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
    health:
      show-details: always

# SpringDoc OpenAPI Documentation
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

---
# Docker Profile
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
      port: ${SPRING_DATA_REDIS_PORT}
  redis:
    host: ${SPRING_DATA_REDIS_HOST}
    port: ${SPRING_DATA_REDIS_PORT}
