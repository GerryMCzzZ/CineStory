<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinestory.mapper.VideoGenerationMapper">

    <select id="selectPendingGenerations" resultType="com.cinestory.model.entity.VideoGeneration">
        SELECT * FROM video_generations
        WHERE status = 'PENDING'
        ORDER BY created_at ASC
    </select>

    <select id="selectTimeoutProcessing" resultType="com.cinestory.model.entity.VideoGeneration">
        SELECT * FROM video_generations
        WHERE status = 'PROCESSING' AND created_at &lt; #{timeoutBefore}
    </select>

    <select id="countByProviderStartingWith" resultType="long">
        SELECT COUNT(*) FROM video_generations
        WHERE provider LIKE CONCAT(#{prefix}, '%')
    </select>

</mapper>
